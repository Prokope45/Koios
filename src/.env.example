# OpenAI-compatible API URL
# For local development: http://127.0.0.1:1234
# For Docker: http://ollama:11434
OPENAI_URL=http://127.0.0.1:1234

# Enable internet search for RAG queries
KOIOS_ENABLE_INTERNET_SEARCH=False

# Hugging Face token (optional, for certain models)
HF_TOKEN=your_huggingface_token_here

# ---------------------------------------------------------------------------
# API Chat History Settings
# ---------------------------------------------------------------------------

# Comma-separated list of approved X-User-ID header values for the REST API.
# Any request to /query or /history without a matching X-User-ID header will
# receive a 401 Unauthorized response.
# Example: KOIOS_APPROVED_USER_IDS=alice,bob,charlie
KOIOS_APPROVED_USER_IDS=

# Filesystem path for the SQLite chat-history database (optional).
# Defaults to db/chat_history.sqlite (same volume as ChromaDB in Docker).
# KOIOS_CHAT_HISTORY_DB_PATH=db/chat_history.sqlite
